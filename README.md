BI503G Assignment 2

This project is made with Nextflow.

main.nf:
Reads a FASTA file and keeps only sequences with GC ≥ 40%.

nextflow.config:
Saves the result file (filtered.fasta) into an output folder.
